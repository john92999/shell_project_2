apiVersion: v1
kind: Service
metadata:
  name: todo-ui-service
  namespace: todo-app
  labels:
    app: todo-ui
spec:
  type: NodePort
  selector:
    app: todo-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30000
