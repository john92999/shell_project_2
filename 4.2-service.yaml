apiVersion: v1
kind: Service
metadata:
  name: todo-api-service
  namespace: todo-app
  labels:
    app: todo-api
spec:
  type: ClusterIP # internal only — browser never calls this directly
  selector:
    app: todo-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
